# 等变DSL正式V1使用说明

## 版本定位

当前可运行版本为`formal-v1.0.0`。它用于验证以下核心路径：

```text
官方Equiformer V1父代
→四个Capability-gated叶子因子
→Factor Router或预注册因子覆盖
→Critic
→Scoped Synthesizer
→Typed Patch
→冻结补集与后端能力检查
→正式等变性门禁
→8k、80k、250k多保真训练
→按Validation选择
→候选冻结后再进行Test评估
```

正式V1开放四个叶子因子：

| 因子 | 含义 | 正式执行路径 |
|---|---|---|
| `F2.2` | 径向编码 | 官方Equiformer V1构造器 |
| `F4.4` | 多头组织 | 官方Equiformer V1构造器 |
| `F5.3` | 归一化与尺度稳定 | 官方Equiformer V1构造器 |
| `F6.3` | 多层readout | 已认证的V1混合后端 |

未认证的通用e3nn计算图和SO(2)融合原型不会进入正式V1排名。

## 关键文件

- 实施计划：`设计文档/正式V1可运行实验实现计划.md`
- 固定配置：`configs/dsl_formal_v1_qm9_alpha_seed201.json`
- 任务合同：`configs/qm9_alpha_formal_v1_task.json`
- 等变协议：`configs/dsl_v1_symmetry_protocol.json`
- Preflight：`scripts/preflight_dsl_formal_v1.py`
- 搜索入口：`scripts/run_dsl_evolution.py`
- 多保真控制器：`scripts/run_dsl_multifidelity_cycles.py`
- 单条启动脚本：`scripts/launch_dsl_formal_v1.sh`
- Supervisor：`scripts/supervise_dsl_formal_v1.sh`

## A100服务器启动

```bash
cd /home/20262202788/equivariant-nas
export EQUIFORMER_PYTHON=/home/20262202788/conda-envs/equiformer/bin/python
bash scripts/launch_dsl_formal_v1.sh /home/20262202788/equivariant-nas/runs/dsl_formal_v1_seed201
```

后台监督运行：

```bash
cd /home/20262202788/equivariant-nas
nohup bash scripts/supervise_dsl_formal_v1.sh \
  /home/20262202788/equivariant-nas/runs/dsl_formal_v1_seed201 \
  >runs/dsl_formal_v1_seed201/supervisor_boot.log 2>&1 &
```

## 可见状态和恢复

Preflight会生成：

```text
READY_TO_LAUNCH.json
STATUS.md
protocol.json
formal_v1_config.json
initial_program.dsl.json
```

搜索和多保真阶段持续保存：

```text
state.json
heartbeat.json
current_candidate.json
evolution.jsonl
checkpoint_last.pth
runtime_manifest.json
protocol.json
```

候选训练中断后，系统优先恢复`current_candidate.json`指向的同一DSL文件和Checkpoint，不重新调用LLM生成替代候选。Supervisor只复用同一Run目录。

## 正式实验协议

| 阶段 | 候选数 | 数据 | batch size | 终点 |
|---|---:|---|---:|---:|
| 第一阶段 | 8 | 固定训练集1/4 | 32 | 8000 steps |
| 第二阶段 | 4 | 同一训练集1/4 | 32 | 80000 steps |
| 第三阶段 | 2 | 完整训练集 | 32 | 250000 steps |

验证间隔为10个data epochs，阶段终点强制Validation。Test不参与搜索或晋级。

## 当前结论边界

正式V1验证的是多因子DSL生成、编译、冻结、等变审计和真实训练工作流。它不宣称已经自动创造Equiformer V2核心算子。`F3.3`、`F5.2`、双因子交互和语言在线进化属于后续V1.1工作。
